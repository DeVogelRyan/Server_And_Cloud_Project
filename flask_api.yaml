apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-k8s-deployment
spec:
  selector:
    matchLabels:
      app: flask-k8s
  template:
    metadata:
      labels:
        app: flask-k8s
    spec:
      containers:
        - name: flask-k8s
          image: docker.io/ryandv123/flask_app
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 6000
